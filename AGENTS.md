# Structural Context

## Repository Setup

This is a wrapper monorepo with a **whitelist gitignore**.

## Subdirectories with Separate Git Remotes

⚠️ **CRITICAL**: Some subdirectories may be independent git repositories with their own remotes. Never attempt to commit changes across subdirectory boundaries.